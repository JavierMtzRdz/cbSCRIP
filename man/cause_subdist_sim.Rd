% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.r
\name{cause_subdist_sim}
\alias{cause_subdist_sim}
\title{Simulate Competing Risks Data from a Mixture Model}
\usage{
cause_subdist_sim(
  n,
  p,
  beta1,
  beta2,
  num.true = 20,
  mix_p = 0.5,
  cor_vals = c(0.7, 0.4, 0.6, 0.5),
  noise_cor = 0.1,
  nblocks = 4,
  lambda1 = 1,
  rho1 = 4,
  lambda2 = 0.8,
  rho2 = 10,
  cens_max = 1.5,
  max_time = 1.5,
  min_time = 1/365,
  exchangeable = FALSE
)
}
\arguments{
\item{n}{Integer, number of subjects to simulate.}

\item{p}{Integer, total number of covariates.}

\item{beta1}{Numeric vector of length \code{p}, coefficients for cause 1.}

\item{beta2}{Numeric vector of length \code{p}, coefficients for cause 2.}

\item{num.true}{Integer, number of non-zero ("true") covariates.}

\item{mix_p}{Numeric (0-1), base probability for the mixture assignment.}

\item{cor_vals}{Numeric vector, correlation for each block in block-diagonal structure.}

\item{noise_cor}{Numeric, the correlation for noise variables.}

\item{nblocks}{Integer, number of blocks for block-diagonal correlation.}

\item{lambda1}{Numeric, the baseline rate parameter for the Weibull distribution of cause 1.}

\item{rho1}{Numeric, the baseline shape parameter for the Weibull distribution of cause 1.}

\item{lambda2}{Numeric, the baseline rate parameter for the Weibull distribution of cause 2.}

\item{rho2}{Numeric, the baseline shape parameter for the Weibull distribution of cause 2.}

\item{cens_max}{Numeric, the maximum time for the uniform censoring distribution.}

\item{max_time}{Numeric, the maximum follow-up time (administrative censoring).}

\item{min_time}{Numeric, the minimum possible event time.}

\item{exchangeable}{Logical, if TRUE, use an exchangeable correlation structure.}
}
\value{
A data.frame with \code{n} rows and \code{p+2} columns ('fstatus', 'ftime',
and covariates X1...Xp).
}
\description{
This function generates competing risks survival data from a mixture model framework.
A subject is first assigned a latent cause of failure, and the event time is
then drawn from a cause-specific Weibull distribution.

\strong{Note:} This method is distinct from and does \strong{not} necessarily produce data
that follows a proportional sub-distribution hazards (Fine & Gray) model.
}
